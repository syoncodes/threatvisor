// sections
import { EndpointCreateView } from 'src/sections/overview/endpoints/view';

// ----------------------------------------------------------------------

export const metadata = {
  title: 'Dashboard: Create a new invoice',
};

export default function InvoiceCreatePage() {
  return <EndpointCreateView />;
}
