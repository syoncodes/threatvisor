// sections
import { EndpointEditView } from 'src/sections/overview/endpoints/view';

// ----------------------------------------------------------------------

export const metadata = {
  title: 'Dashboard: Create a new invoice',
};

export default function InvoiceEditPage() {
  return <EndpointEditView />;
}
